CREATE TABLE admins(
ID TEXT PRIMARY KEY NOT NULL,
USERNAME TEXT NOT NULL
);
CREATE TABLE roles(
ID INTEGER PRIMARY KEY,
name TEXT NOT NULL
);
CREATE TABLE users(
ID TEXT PRIMARY KEY NOT NULL,
USERNAME TEXT NOT NULL,
ROLES TEXT NOT NULL
);
CREATE UNIQUE INDEX roles_name on roles(name);
CREATE UNIQUE INDEX users_idx on users(id,username);

